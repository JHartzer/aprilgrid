# aprilgrid-cpp

A basic c++ library for performing april grid detections and pose estimations using OpenCV

## Build
```
mkdir build -p
cd build
cmake ../
cmake --build .
```

## Test
```
cd build
./aprilgrid-cpp-test
```